# Personal AI Usage Rules (Module 5)

| Rule category | Draft rule | Evidence from my notes | What is still vague? | Revised rule |
|---|---|---|---|---|
| What I will never paste | I will never paste project-specific internal security posture details into AI tools. | All shared items are marked Medium because they expose internal implementation/security context (S-01 to S-06), and each row includes a safer generalized version. | "Project-specific" is broad and hard to enforce unless I define what counts. | I will not paste exact internal findings from my repo (for example: exact auth gaps, exact storage failure paths, exact CORS/base-URL assumptions, exact CI security-control gaps); I will only paste generalized wording like the "Safer future version" text from my worksheet. |
| What I will always verify before accepting | Before I accept AI output, I will always verify that it does not include specific internal environment or implementation details and that ambiguities are resolved. | Every row has an "Ambiguity to resolve" field (repo visibility, deployment details, whether logs/paths were shared, and scope of dependency/workflow details). | "Resolve ambiguities" is vague unless I list the exact checks. | Before using AI output, I will check these five items and stop if any are unknown: repo visibility, deployment/environment details included, stack trace/filesystem path leakage, hostnames/ports exposure, and whether dependency/workflow details are more specific than needed; if unknown, mark "Missing - add course evidence" and do not publish. |
| How I will record AI contributions | I will document each AI-assisted item in a table with risk and safer wording. | Your worksheet already uses a structured table: Item shared, Risk, Reason, Safer future version, Ambiguity to resolve. | Missing - add course evidence for where this log must live and when entries must be updated. | For each AI interaction I keep, I will add one row to a worksheet with exactly these fields: Item shared, Risk, Reason, Safer future version, Ambiguity to resolve; I will update the row when ambiguity is resolved, and if I cannot support a claim from notes I will write "Missing - add course evidence." |
